import {createMuiTheme} from '@material-ui/core/styles';

const Theme = createMuiTheme({
    palette : {
        primary : {
            light : "#63afff",
            main : "#1976d2",
            dark : "#004ba0",
            contrastText : "#ecfad8"

        }
    },


});

export default Theme;